Для запуска необходимо утановить Docker. 
Команды для запуска:
  docker-compose build
  docker-compose up -d
Приложение запустится в фоне.
Автоматически поднимутся контейнеры с postgres и golang приложением
